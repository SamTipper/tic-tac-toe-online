import { ShowPiecePipe } from './show-piece.pipe';

describe('ShowPiecePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowPiecePipe();
    expect(pipe).toBeTruthy();
  });
});
