import { PieceColorPipe } from './piece-color.pipe';

describe('PieceColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PieceColorPipe();
    expect(pipe).toBeTruthy();
  });
});
